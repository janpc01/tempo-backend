spring:
  application:
    name: tempo
  # config:
  #   import: 
  #     - aws-secretsmanager:POSTGRESQL_HOST
  #     - aws-secretsmanager:POSTGRESQL_USERNAME
  #     - aws-secretsmanager:POSTGRESQL_PASSWORD
  #     - aws-secretsmanager:POSTGRESQL_DB
  # cloud:
  #   aws:
  #     secrets-manager:
  #       enabled: true
  datasource:
    url: jdbc:postgresql://${POSTGRESQL_USERNAME.value}:${POSTGRESQL_PASSWORD.value}@${POSTGRESQL_HOST.value}/${POSTGRESQL_DB.value}?sslmode=require
    username: ${POSTGRESQL_USERNAME.value}
    password: ${POSTGRESQL_PASSWORD.value}
logging:
  level:
    org.springframework: ERROR
    org.hibernate: ERROR
    org.flywaydb: ERROR
    org.postgresql: ERROR
    org.testcontainers: ERROR
    io.awspring.cloud: ERROR
