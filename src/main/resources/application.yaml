spring:
  application:
    name: tempo
  # config:
  #   import: 
  #     - aws-secretsmanager:POSTGRESQL_HOST
  #     - aws-secretsmanager:POSTGRESQL_USERNAME
  #     - aws-secretsmanager:POSTGRESQL_PASSWORD
  #     - aws-secretsmanager:POSTGRESQL_DB
  # cloud:
  #   aws:
  #     secrets-manager:
  #       enabled: true
  datasource:
    url: jdbc:postgresql://${POSTGRESQL_USERNAME.POSTGRESQL_USERNAME}:${POSTGRESQL_PASSWORD.POSTGRESQL_PASSWORD}@${POSTGRESQL_HOST.POSTGRESQL_HOST}/${POSTGRESQL_DB.POSTGRESQL_DB}?sslmode=require
    username: ${POSTGRESQL_USERNAME.POSTGRESQL_USERNAME}
    password: ${POSTGRESQL_PASSWORD.POSTGRESQL_PASSWORD}
logging:
  level:
    org.springframework: ERROR
    org.hibernate: ERROR
    org.flywaydb: ERROR
    org.postgresql: ERROR
    org.testcontainers: ERROR
    io.awspring.cloud: ERROR
